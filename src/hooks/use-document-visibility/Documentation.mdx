import { Canvas, Controls, Meta, Source, Story } from '@storybook/blocks';

import * as allStories from './use-document-visibility.stories';

<Meta of={allStories} />

# useDocumentVisibility

`useDocumentVisibility` is a React hook that returns the current document visibility.

## Usage

### Example

<Canvas of={allStories.Usage} />

### Code

<Source of={allStories.Usage} />

## Parameters

`useDocumentVisibility` accepts no parameters.

## Returns

`useDocumentVisibility` returns a string representing the current document visibility.
