import { Canvas, Controls, Meta, Source, Story } from '@storybook/blocks';

import * as allStories from './use-document-title.stories';

<Meta of={allStories} />

# useDocumentTitle

`useDocumentTitle` is a React hook that allows you to set the document title.

## Usage

### Example

<Canvas of={allStories.Usage} />

### Code

<Source of={allStories.Usage} />

## Parameters

- `title` - The title to set.

## Returns

`useDisclosure` returns nothing.
